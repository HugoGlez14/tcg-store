"use client";

import "./admin.css";
import "./admin-enhancements.css";
import Link from "next/link";
import { useEffect, useState } from "react";

const tabs = ["Resumen", "Productos", "Imágenes", "Pedidos", "Pagos", "Ajustes"];

export default function AdminPage() {
  const [tab, setTab] = useState("Resumen");
  const [saved, setSaved] = useState("");
  const save = (message: string) => { setSaved(message); setTimeout(() => setSaved(""), 2500); };
  return <main className="admin-page"><aside className="admin-sidebar"><Link className="wordmark" href="/">[ ] <span>TCG STORE</span></Link><div className="admin-caption">Administración</div><nav>{tabs.map((item) => <button className={tab === item ? "selected" : ""} key={item} onClick={() => setTab(item)}>{item}<span>{item === "Productos" ? "12" : item === "Pedidos" ? "3" : ""}</span></button>)}</nav><div className="admin-user"><b>Administrador</b><span>Acceso principal</span><Link href="/">← Ver tienda</Link></div></aside><section className="admin-main"><header className="admin-top"><div><p>Panel de control</p><h1>{tab}</h1></div><button className="publish" onClick={() => save("Cambios guardados como borrador")}>Guardar cambios</button></header>{tab === "Resumen" && <Overview setTab={setTab} />}{tab === "Productos" && <EditableProductForm save={save} />}{tab === "Imágenes" && <ImageManager save={save} />}{tab === "Pagos" && <Payments save={save} />}{tab === "Pedidos" && <Orders />}{tab === "Ajustes" && <Settings save={save} />}</section>{saved && <output className="admin-toast">✓ {saved}</output>}</main>;
}

function Overview({ setTab }: { setTab: (tab: string) => void }) { return <><section className="metrics"><article><small>Ventas del mes</small><strong>$0.00</strong><span>Se activa al conectar pagos</span></article><article><small>Pedidos pendientes</small><strong>03</strong><span>Requieren revisión</span></article><article><small>Productos publicados</small><strong>12</strong><span>3 catálogos activos</span></article></section><section className="admin-grid"><article className="admin-card wide"><div className="card-heading"><div><p>Acceso rápido</p><h2>Prepara tu tienda para vender.</h2></div></div><div className="quick-actions"><button onClick={() => setTab("Productos")}>＋ Añadir producto</button><button onClick={() => setTab("Pagos")}>◉ Configurar pagos</button><button onClick={() => setTab("Ajustes")}>⚙ Ajustes de tienda</button></div></article><article className="admin-card"><p>Estado de lanzamiento</p><div className="launch-progress"><i /><i /><i /><i /></div><ol><li className="done">Catálogos creados</li><li className="done">Estructura de productos</li><li>Conectar pagos</li><li>Publicar inventario</li></ol></article></section></>; }

type EditableProduct = { id: number; name: string; game: string; category: string; price: string; stock: string; status: "Publicado" | "Borrador"; description: string };
const initialProducts: EditableProduct[] = [
  { id: 1, name: "Destined Rivals — ETB", game: "Pokémon", category: "Expansiones", price: "1,499.00", stock: "8", status: "Publicado", description: "Caja Elite Trainer de Destined Rivals." },
  { id: 2, name: "Spiritforged — Booster Box", game: "Riftbound", category: "Producto sellado", price: "0.00", stock: "0", status: "Borrador", description: "Booster Box de Spiritforged." },
  { id: 3, name: "Alliance Insight — Caja", game: "Yu-Gi-Oh!", category: "Producto sellado", price: "1,850.00", stock: "5", status: "Publicado", description: "Caja de Alliance Insight." },
];
const emptyProduct: Omit<EditableProduct, "id"> = { name: "", game: "Pokémon", category: "Expansiones", price: "", stock: "0", status: "Borrador", description: "" };

function EditableProductForm({ save }: { save: (message: string) => void }) {
  const [products, setProducts] = useState(initialProducts);
  const [draft, setDraft] = useState<Omit<EditableProduct, "id">>(emptyProduct);
  const [editingId, setEditingId] = useState<number | null>(null);
  const update = (field: keyof Omit<EditableProduct, "id">, value: string) => setDraft((current) => ({ ...current, [field]: value }));
  const edit = (product: EditableProduct) => { const { id, ...values } = product; setDraft(values); setEditingId(id); window.scrollTo({ top: document.body.scrollHeight, behavior: "smooth" }); };
  const cancel = () => { setDraft(emptyProduct); setEditingId(null); };
  const submit = (status: "Publicado" | "Borrador") => { if (!draft.name.trim()) { save("Escribe el nombre del producto antes de guardar"); return; } const record = { ...draft, status }; if (editingId) { setProducts((items) => items.map((product) => product.id === editingId ? { ...record, id: editingId } : product)); save("Producto actualizado"); } else { setProducts((items) => [...items, { ...record, id: Date.now() }]); save("Producto agregado al listado"); } cancel(); };
  const remove = (id: number) => { setProducts((items) => items.filter((product) => product.id !== id)); if (editingId === id) cancel(); save("Producto eliminado del listado"); };
  return <><section className="admin-card product-list"><div className="card-heading"><div><p>Inventario</p><h2>Productos cargados</h2></div><button className="add-product" onClick={cancel}>＋ Agregar</button></div><div className="product-row product-row-head"><span>Producto</span><span>TCG</span><span>Stock</span><span>Estado</span><span>Acciones</span></div>{products.map((product) => <div className="product-row" key={product.id}><b>{product.name}</b><span>{product.game}</span><span>{product.stock}</span><em className={product.status === "Publicado" ? "published" : "draft"}>{product.status}</em><div><button className="mini-edit" onClick={() => edit(product)}>Editar</button><button className="mini-delete" onClick={() => remove(product.id)}>Eliminar</button></div></div>)}</section><section className="product-editor"><div className="editor-header"><div><p>{editingId ? "Editar producto" : "Nuevo producto"}</p><h2>{editingId ? draft.name || "Producto" : "Información y publicación"}</h2></div><span>{draft.status}</span></div><div className="editor-grid"><section className="form-panel"><label>Nombre del producto<input value={draft.name} onChange={(event) => update("name", event.target.value)} placeholder="Ej. Elite Trainer Box" /></label><div className="form-row"><label>Juego<select value={draft.game} onChange={(event) => update("game", event.target.value)}><option>Pokémon</option><option>Riftbound</option><option>Yu-Gi-Oh!</option></select></label><label>Categoría<select value={draft.category} onChange={(event) => update("category", event.target.value)}><option>Expansiones</option><option>Producto sellado</option><option>Individuales</option><option>Accesorios</option></select></label></div><div className="form-row"><label>Precio MXN<input value={draft.price} onChange={(event) => update("price", event.target.value)} placeholder="$ 0.00" inputMode="decimal" /></label><label>Inventario<input value={draft.stock} onChange={(event) => update("stock", event.target.value)} inputMode="numeric" /></label></div><label>Descripción<textarea value={draft.description} onChange={(event) => update("description", event.target.value)} placeholder="Detalles importantes: idioma, expansión, condición y contenido." /></label><div className="form-buttons">{editingId && <button className="secondary" onClick={cancel}>Cancelar</button>}<button className="secondary" onClick={() => submit("Borrador")}>Guardar borrador</button><button className="primary-action" onClick={() => submit("Publicado")}>{editingId ? "Guardar cambios" : "Guardar y publicar"}</button></div></section><section className="upload-panel"><p>Imágenes del producto</p><div className="dropzone"><strong>Portada y galería</strong><span>Sube las imágenes desde el panel Imágenes al conectar la base de datos.</span><small>La primera foto será la portada del producto.</small></div></section></div></section></>;
}

function ProductForm({ save }: { save: (message: string) => void }) { const [products, setProducts] = useState([{ id: 1, name: "Destined Rivals — ETB", game: "Pokémon", stock: 8, status: "Publicado" }, { id: 2, name: "Spiritforged — Booster Box", game: "Riftbound", stock: 0, status: "Borrador" }, { id: 3, name: "Alliance Insight — Caja", game: "Yu-Gi-Oh!", stock: 5, status: "Publicado" }]); const remove = (id: number) => { setProducts((items) => items.filter((product) => product.id !== id)); save("Producto eliminado del listado"); }; return <><section className="admin-card product-list"><div className="card-heading"><div><p>Inventario</p><h2>Productos cargados</h2></div><button className="add-product" onClick={() => save("Completa el formulario para añadir un producto")}>＋ Agregar</button></div><div className="product-row product-row-head"><span>Producto</span><span>TCG</span><span>Stock</span><span>Estado</span><span>Acciones</span></div>{products.map((product) => <div className="product-row" key={product.id}><b>{product.name}</b><span>{product.game}</span><span>{product.stock}</span><em className={product.status === "Publicado" ? "published" : "draft"}>{product.status}</em><div><button className="mini-edit" onClick={() => save(`Editando ${product.name}`)}>Editar</button><button className="mini-delete" onClick={() => remove(product.id)}>Eliminar</button></div></div>)}</section><section className="product-editor"><div className="editor-header"><div><p>Nuevo producto</p><h2>Información y publicación</h2></div><span>Borrador</span></div><div className="editor-grid"><section className="form-panel"><label>Nombre del producto<input placeholder="Ej. Elite Trainer Box" /></label><div className="form-row"><label>Juego<select defaultValue=""><option value="" disabled>Seleccionar TCG</option><option>Pokémon</option><option>Riftbound</option><option>Yu-Gi-Oh!</option></select></label><label>Categoría<select defaultValue=""><option value="" disabled>Seleccionar categoría</option><option>Expansiones</option><option>Producto sellado</option><option>Individuales</option><option>Accesorios</option></select></label></div><div className="form-row"><label>Precio MXN<input placeholder="$ 0.00" inputMode="decimal" /></label><label>Inventario<input placeholder="0" inputMode="numeric" /></label></div><label>Condición<select defaultValue="Nuevo"><option>Nuevo / sellado</option><option>Near Mint</option><option>Lightly Played</option><option>Moderately Played</option></select></label><label>Descripción<textarea placeholder="Detalles importantes: idioma, expansión, condición y contenido." /></label><div className="form-buttons"><button className="secondary" onClick={() => save("Borrador guardado")}>Guardar borrador</button><button className="primary-action" onClick={() => { setProducts((items) => [...items, { id: Date.now(), name: "Nuevo producto", game: "Por asignar", stock: 0, status: "Borrador" }]); save("Producto agregado al listado"); }}>Guardar y publicar</button></div></section><section className="upload-panel"><p>Imágenes del producto</p><label className="dropzone"><input type="file" accept="image/*" multiple /><strong>Arrastra imágenes aquí</strong><span>o selecciona archivos desde tu equipo</span><small>JPG, PNG o WebP · hasta 8 imágenes</small></label><div className="image-slots"><span>01</span><span>02</span><span>03</span><span>＋</span></div><small className="upload-tip">La primera foto será la portada del producto.</small></section></div></section></>; }

const coverOptions = [{ slug: "pokemon", name: "Pokémon", hint: "Banner para el catálogo de Pokémon" }, { slug: "riftbound", name: "Riftbound", hint: "Banner para el catálogo de Riftbound" }, { slug: "yugioh", name: "Yu-Gi-Oh!", hint: "Banner para el catálogo de Yu-Gi-Oh!" }];

function ImageManager({ save }: { save: (message: string) => void }) { const [covers, setCovers] = useState<Record<string, string>>({}); useEffect(() => { setCovers(Object.fromEntries(coverOptions.map(({ slug }) => [slug, localStorage.getItem(`tcg-cover-${slug}`) || ""]))); }, []); const upload = (slug: string, file?: File) => { if (!file) return; if (!file.type.startsWith("image/")) { save("Elige un archivo de imagen válido"); return; } if (file.size > 3 * 1024 * 1024) { save("La imagen debe pesar menos de 3 MB"); return; } const reader = new FileReader(); reader.onload = () => { const value = String(reader.result); localStorage.setItem(`tcg-cover-${slug}`, value); setCovers((current) => ({ ...current, [slug]: value })); save(`Portada de ${coverOptions.find((cover) => cover.slug === slug)?.name} actualizada`); }; reader.readAsDataURL(file); }; const clear = (slug: string) => { localStorage.removeItem(`tcg-cover-${slug}`); setCovers((current) => ({ ...current, [slug]: "" })); save("Portada eliminada"); }; return <section className="image-manager"><div className="payment-intro"><div><p>Biblioteca visual</p><h2>Portadas por TCG.</h2><span>Sube una portada independiente para cada catálogo. Se mostrará al instante en su página correspondiente.</span></div><b>JPG · PNG · WebP</b></div><div className="cover-grid">{coverOptions.map((cover) => <article className="cover-card" key={cover.slug}><div className={`cover-preview ${covers[cover.slug] ? "has-image" : ""}`} style={covers[cover.slug] ? { backgroundImage: `url(${covers[cover.slug]})` } : undefined}><span>{covers[cover.slug] ? "Portada cargada" : cover.name}</span></div><div><h3>{cover.name}</h3><p>{cover.hint}</p></div><label className="cover-upload">Cambiar portada<input type="file" accept="image/png,image/jpeg,image/webp" onChange={(event) => upload(cover.slug, event.target.files?.[0])} /></label>{covers[cover.slug] && <button className="mini-delete" onClick={() => clear(cover.slug)}>Quitar imagen</button>}</article>)}</div><section className="admin-card image-guidance"><p>Imágenes de productos</p><span>En la sección <b>Productos</b> puedes subir varias fotos por artículo. La primera será su portada; las demás quedarán disponibles para su galería.</span></section></section>; }

function Payments({ save }: { save: (message: string) => void }) { const methods = [["Mercado Pago", "Recomendado para México", "Tarjetas, SPEI y efectivo"], ["Stripe", "Pagos con tarjeta", "Visa, Mastercard y AMEX"], ["PayPal", "Pago desde cuenta", "Protección para compradores"], ["Transferencia SPEI", "Pago manual", "Validación antes de envío"]]; return <section className="payments"><div className="payment-intro"><div><p>Métodos de pago</p><h2>Cobra de forma segura.</h2><span>Conecta solo los métodos que usarás. Las credenciales se guardarán como variables privadas y nunca se mostrarán a compradores.</span></div><b>Entorno de preparación</b></div><div className="payment-grid">{methods.map(([name, subtitle, detail], index) => <article key={name} className="payment-card"><div className="payment-logo">{name.slice(0, 2).toUpperCase()}</div><div><h3>{name}</h3><p>{subtitle}</p><small>{detail}</small></div><button onClick={() => save(`${name}: configuración pendiente de credenciales`)}>Configurar <span>→</span></button>{index === 0 && <em>Recomendado</em>}</article>)}</div><section className="payment-checklist"><h3>Antes de activar cobros</h3><ul><li>Cuenta de negocio verificada en la plataforma elegida.</li><li>Política de privacidad, términos y devoluciones publicadas.</li><li>Cuenta bancaria para recibir depósitos.</li><li>Prueba de compra en modo sandbox antes de publicar.</li></ul></section></section>; }

function Orders() { return <section className="admin-card orders"><div className="card-heading"><div><p>Pedidos</p><h2>Revisión de pedidos</h2></div><button>Exportar</button></div><div className="order-row header"><span>Pedido</span><span>Cliente</span><span>Total</span><span>Estado</span></div>{["#0003", "#0002", "#0001"].map((order, index) => <div className="order-row" key={order}><b>{order}</b><span>{index === 0 ? "Pago por validar" : "Cliente de prueba"}</span><strong>{index === 0 ? "—" : "$0.00"}</strong><em>{index === 0 ? "Pendiente" : "Borrador"}</em></div>)}</section>; }

function Settings({ save }: { save: (message: string) => void }) { const [homeIntro, setHomeIntro] = useState(""); useEffect(() => { setHomeIntro(localStorage.getItem("tcg-home-intro") || ""); }, []); const saveHomeIntro = () => { localStorage.setItem("tcg-home-intro", homeIntro.trim()); save(homeIntro.trim() ? "Texto de inicio actualizado" : "Texto de inicio eliminado"); }; return <section className="settings"><article className="admin-card"><p>Datos de la tienda</p><label>Nombre visible<input placeholder="Pendiente de definir" /></label><label>Correo de atención<input type="email" placeholder="correo@tutienda.com" /></label><button className="primary-action" onClick={() => save("Datos de tienda guardados")}>Guardar datos</button></article><article className="admin-card"><p>Texto de la página principal</p><span>Agrega una frase breve debajo de “Elige tu universo” o déjalo vacío para ocultarla.</span><label>Mensaje de bienvenida<input value={homeIntro} onChange={(event) => setHomeIntro(event.target.value)} placeholder="Ej. Cartas, comunidad y grandes hallazgos." maxLength={120} /></label><button className="primary-action" onClick={saveHomeIntro}>Guardar texto de inicio</button></article><article className="admin-card"><p>Envíos y políticas</p><span>Configura zonas, tarifas, tiempos de entrega, devoluciones y textos legales antes de abrir ventas.</span><button className="secondary" onClick={() => save("Políticas preparadas para edición")}>Editar políticas</button></article></section>; }
